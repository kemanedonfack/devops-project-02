pipeline {
    agent any
    
    stages {
        
        stage('ansible') {
            steps {
                dir('ansible') {
                  sh 'ls'
                  ansiblePlaybook installation: 'ansible', inventory: 'hosts', playbook: 'deploy-playbook.yml'
                }
            }
        }

    }
}
