pipeline {
    agent any
    
    stages {
        
        stage('ansible') {
            steps {
                dir('ansible') {
                  sh 'ls'
                  ansiblePlaybook become: true, credentialsId: 'masternodeid', installation: 'ansible', inventory: 'hosts', playbook: '../kubernetes/deploy-playbook.yml'
                }
            }
        }

    }
}
